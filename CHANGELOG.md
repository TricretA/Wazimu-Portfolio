# Changelog

## Unreleased

- Added the Privacy Policy (`/privacy`) and Terms of Service (`/terms`) pages,
  linked from the footer between GitHub and CV. Both are built as their own
  static entries so the URLs resolve without a host rewrite rule — platform
  reviewers such as Meta's WhatsApp Business API request them cold.
- Added the VacaSky Adventure Travel Website case study, with a captured hero image, live-site link, and public source link.
