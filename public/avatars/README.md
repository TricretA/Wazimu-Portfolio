Drop client headshots or company marks here (square, ideally 200x200 or larger),
then point at them from `photo` in src/data/testimonials.ts — e.g. "/avatars/bernard-kimathi.jpg".

Any testimonial left with `photo: null` renders an initials disc instead.
