Project screenshots, 16:10, referenced by `image` in src/data/problems.ts.

Still needed (private mobile apps — client screenshots pending):
  scope-sms, skylink-bingwa

Also missing, because the deployment is gone:
  kuccps-course-checker  (kuccpscoursechecker.co.ke no longer resolves)
  bingwa-posters         (site loads but its template API errors out)
  whatsapp-bundle-automation (bfasta.vercel.app returns 404)

Save each as <slug>.webp, then set `image: '/work/<slug>.webp'` on that project.
